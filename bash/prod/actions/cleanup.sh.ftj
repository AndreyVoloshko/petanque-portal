#!/bin/bash

{% for node in swarm_nodes %}

eval "$(docker-machine env {{ node }} --shell=bash)"

docker image prune -a -f
docker container prune -f
docker ps -a | grep Exited | awk '{ print $1 }' | xargs docker rm -v

{% endfor %}